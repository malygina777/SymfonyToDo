<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container">
    
    
    <a class="navbar-brand d-flex align-items-center text-white" href="{{ path('app_kanban') }}">
          <img
            src="{{ asset('images/logo.svg') }}"
            alt="Tableau Kanban"
            width="28" height="28"
            class="brand-logo me-2"
          >
          TodoList
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div id="mainNav" class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto">
        {# {% if app.user %} #}
         
          <li class="nav-item"><a class="nav-link" href="{{path('app_task_index')}}">Mes tâches</a></li>
          <li class="nav-item"><a class="nav-link" href="{{path('app_task_new')}}">Créer une tâche</a></li>
          <li class="nav-item"><a class="nav-link" href="#">Tâches partagées</a></li>
        {# {% endif %} #}
      </ul>

      <ul class="navbar-nav ms-auto">
        {% if app.user %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
              {{ app.user.username ?? app.user.email }}
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              {# <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li> #}
              <li><a class="dropdown-item" href="#">Déconnexion</a></li>
            </ul>
          </li>
        {% else %}
          {# <li class="nav-item"><a class="btn btn-outline-light me-2" href="{{ path('app_login') }}">Connexion</a></li>
          <li class="nav-item"><a class="btn btn-warning" href="{{ path('app_register') }}">Inscription</a></li> #}
          <li class="nav-item"><a class="btn btn-outline-light me-2" href="#">Connexion</a></li>
          <li class="nav-item"><a class="btn btn-warning" href="#">Inscription</a></li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
